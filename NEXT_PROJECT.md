# 下一個專案：統一旅遊帳

冰島版 + 台灣版合併成一個通用的旅遊記錄／記帳工具。

**時機：冰島回來（2026/10）之後才動工。** 出發前不要開新坑。

---

## 產品樣貌（使用者自己描述的原話，這是規格）

> 一本書，翻開，看見記錄著我過去的旅程總覽，點一下進入過去旅程的詳細規劃。
> 網頁某處有按鈕可以新增旅程。
>
> 但有分大旅行跟國內小旅行，那這種可能首頁有兩本書可以選，小本的就是國內的手札。
>
> 但出國的大旅行需要準備，所以會好好處理，但**小旅行會比較隨興，可能會懶得紀錄**。
>
> 兩本書打開都是一張地圖，一世界地圖，一台灣地圖。

### 從這段話推出的設計決定

1. **首頁＝書桌，上面兩本書**
   - 厚的：出國旅行（深藍布面、燙金字）
   - 薄的：島內手札（卡其、矮一截、書脊也薄）
   - 「重量」的差別要看得出來，讀者才不會期待小本也要填一堆表

2. **翻開＝地圖，不是列表**
   - 出國 → 世界地圖；島內 → 台灣地圖
   - **地圖上的標記就是目錄**，點下去進入該趟旅程；列表只是備援
   - 不能載外部圖磚（CSP 擋掉），要自己畫；點陣風格剛好跟現有像素風一致

3. **「懶得紀錄」是設計條件，不是缺點**
   - 大本：先規劃 → 執行 → 封存（行程／住宿／航班／分帳／行前準備）
   - 小本：**摩擦力必須極低**——只問「去哪」＋「一句話」，日期預設今天，
     連「建立旅程」這一步都不該有
   - 兩邊的新增入口用字也不同：「規劃新的旅程」vs「記一筆」
   - 條目字級也分：大本用襯線大字（章節感），小本用一般字（隨手寫的一行）

4. **新增旅程的入口** 放在書頁最後一列，長得像「還沒寫的那一頁」

### 原型

已做出可點的原型（書桌 → 地圖書頁 → 內頁空殼），資料存 localStorage：
https://claude.ai/code/artifact/0d6c7518-72bb-4443-8b09-46559c5c3f42

---

## 核心架構：生命週期驅動

```
規劃中  →  進行中  →  已封存
  ↓          ↓          ↓
 要寫入    要寫入      唯讀
           +離線     靜態 JSON
```

- **同時最多只有一趟旅程是「活的」**
- 已封存的旅程 → 匯出成靜態 JSON → GitHub Pages → **永久免費、不會消失**
- 後端只服務「規劃中 + 進行中」的旅程
- **旅程空窗期可以把服務關掉**，封存資料照樣看得到

這是整個成本模型的基礎：一年只有幾週真的需要活的後端。

## 技術選型

| 項目 | 選擇 | 理由 |
|---|---|---|
| 部署 | **Railway**（已有訂閱，現用約 $1/月）| Supabase 免費版閒置會休眠，旅遊間歇性使用不適合 |
| 資料庫 | **SQLite on volume** | Postgres 是獨立服務會 24h 吃記憶體；SQLite 跑在 Node 服務裡，一個服務取代兩個 |
| 前端 | 沿用 vanilla JS，無建置流程 | 現有專案就是這樣，穩定、好維護 |
| 封存 | 靜態 JSON + GitHub Pages | $0、永久 |

**待確認**：Railway 現行費率、加一個服務後是否仍在 $5 額度內（去後台看 Usage）。

## 資料模型

### 關鍵修正：分攤要獨立成表

現在冰島版把成員寫死在欄位名（「猴負擔」「花負擔」「寧負擔」），這是最大的技術債，
通用化一定要改掉：

```
expense (記帳)
  └── expense_split (分攤明細)
        ├── member_id
        └── amount
```

這樣成員可以任意增減，不用改欄位、不用改公式。

### 實體清單

| 實體 | 說明 | 來源 |
|---|---|---|
| trip | 旅程容器：名稱／起訖／地區／幣別／狀態／預算 | 台灣版已有雛形 |
| member | 成員：名稱／顏色／sprite／emoji，**綁在 trip 底下** | 台灣版已有角色庫 |
| expense + expense_split | 記帳與分攤 | 冰島版（要重構分攤）|
| checkin | 打卡：座標／時間／成員／塗鴉／留言 | 兩版都有 |
| schedule | 日程：日期／時間／分類／標題／座標／停留／網址 | 冰島版 |
| stay | 住宿 | 冰島版 |
| activity | 活動 | 冰島版 |
| flight | 航班（多航段）| 冰島版 |
| task | 行前準備：分類／重要度／對象／各成員狀態 | 冰島版 |
| manual | 手冊：分類／標題／內容／連結 | 冰島版 |
| insurance | 保險 | 冰島版 |

## 前端關鍵設計：schema-driven

**不要寫 11 套 CRUD 表單。** 把每種實體的欄位定義成設定檔，表單／列表／驗證自動生成：

```js
stay: [
  { key:'name',  label:'住宿地點', type:'text',   required:true },
  { key:'date',  label:'日期',     type:'date',   required:true },
  { key:'price', label:'價格',     type:'number' },
  { key:'lat',   label:'緯度',     type:'number', group:'進階' },
]
```

**這同時解決「未來旅程通用」的需求**——欄位不一樣就改設定檔，不用改程式。

## 從冰島版帶走的資產

**要帶走**（踩坑換來的）：
- **離線佇列 + 樂觀更新** — 荒野沒訊號時的命脈
- **快取先渲染** — 先畫舊資料再抓新的
- **像素風角色系統 / 地圖 / 時間軸** — 這是靈魂，跟資料層無關
- **失敗一律進佇列** — 不能因為分不出錯誤類型就把使用者輸入弄丟

**要改掉**：
- 分攤欄位寫死成員名
- 一次只能一個旅程
- 日程／地圖各有一套日期解析與排序邏輯（兩邊要共用）

## 遷移路徑（2026-08-31 修訂：平行建置、分批封存）

**原則：不改現有檔案。新的東西長在新資料夾，舊的等新的能用了才封存。**
這樣行前不會動到正在使用的 iceland/，也不用一次大搬遷。

### 共用模組怎麼抽

**不要「先抽共用、再拿來用」——介面會是憑想像定的，之後要改兩次。**
正確做法是**一邊寫新的台灣小本、共用模組自己長出來**：
book/ 已經是第一個使用者，新小本是第二個，凡是兩邊都要的就當場提到 `shared/`。
介面被真的用過才定案。

## 舊台灣版需求盤點 → 新台灣小本規劃（2026-08-31，先討論不實作）

### 一、舊版實際做了什麼（從 taiwan/index.html 2674 行裡挖出來的完整清單）

每項標了去向：**留**（進新小本）／**共**（上繳共用模組）／**大**（屬於大本）／**淘**（不帶走）。

**地圖與打卡**
| 功能 | 細節 | 去向 |
|---|---|---|
| Leaflet 地圖＋三種風格 | 水果/明亮/暗色（CSS filter 換色） | 留，但風格改由主題系統管（共） |
| 🐾 打卡 | GPS 定位或地圖點選、地點名稱選填（預設座標）、一句話(note)、塗鴉板、成員 | 留＝「記一筆」，成員改 author+with |
| 💗 想去 | 同打卡但是願望，之後可「去過了，打卡！」轉正 | 留，轉正流程好用要保 |
| 長按地圖快速標記 | showQuickTag：跳出 打卡/想去 兩鍵 | 留，摩擦力低的精華 |
| 圖層開關 | 打卡/想去/行前點/記帳 各自顯示切換 | 留（簡化：新版點型只剩 停留/途經/想去） |
| 底部抽屜卡片列表 | 手機下拉抽屜、桌機側欄；分頁籤 全部/打卡/想去 | 淘，被「出遊卡」取代 |
| 點卡片飛到地圖 | flyTo ＋ popup | 留（出遊卡 ↔ 地圖連動，book/ 已有雛形） |

**記帳**
| 功能 | 細節 | 去向 |
|---|---|---|
| 💰 記帳（塞在打卡表單裡） | 金額/項目/分類/付款人，不分帳 | 共＝記帳引擎 splitMode:none，介面移出打卡表單 |
| 🧾 帳本 modal | 列表＋點項目飛地圖 | 共（記帳引擎的檢視） |

**旅程（小旅行也要建旅程——這就是舊版最大的錯）**
| 功能 | 細節 | 去向 |
|---|---|---|
| 🧳 建旅程 | 名稱/日期/成員 | 淘，被「出遊自動成串」取代 |
| 行前 QA 問答 → 小卡 | 問幾題生成行前備忘卡、可標地點上地圖 | 大（行前準備本來就是大本的事） |
| 旅程日曆 tcal | 月曆選日期 | 淘（出遊卡按時間排就夠） |
| 旅程篩選 | 只看某旅程的點 | 留＝點開出遊卡就是這功能 |

**成員與其他**
| 功能 | 細節 | 去向 |
|---|---|---|
| 名冊/角色編輯/新增臨時成員 | sprite/顏色/emoji | 共✅ 角色館已做 |
| 💬 留言 | 每個點可留言（多人用） | 淘：改個人制後沒有別人來留言；「我想補一句」＝編輯 note |
| 塗鴉板 | pixelpad | 共✅ 已在 shared/ |
| 塗鴉上傳 R2 成圖片 | toPNGBlob → worker | 留（也是未來相片功能的通道） |
| stats.html 足跡回顧 | 縣市著色地圖＋各縣市打卡數＋成員條 | 留，但併進小本（縣市足跡是島內獨有的成就感） |
| PWA/SW 快取 | manifest+sw | 留（新小本也要能出門用） |
| 編輯/刪除打卡 | 改 note/塗鴉/所屬旅程 | 留 |

### 二、這份清單說出的需求本質

1. **記錄要快**（GPS、長按快標、選填地名）——舊版做對的地方，全保留。
2. **回顧要有單位**——舊版只有散點和抽屜列表，所以茫然；新版單位是出遊卡。
3. **記帳是順手記，不是分帳**——島內從頭到尾 splitMode:none，介面卻抄了出國版的複雜度。
4. **旅程功能是錯位**——行前 QA、旅程表單、日曆都是「大旅行的器官」長在小本上。
5. **縣市足跡是島內獨有的樂趣**——世界地圖看國家，台灣地圖看縣市，stats 該是一等公民不是另一頁。

### 三、新台灣小本的介面規劃（notes/，動工前的藍圖）

**三個畫面，沒有 modal 迷宮：**

```
[手札首頁]                    [出遊卡]                  [記一筆]
台灣地圖(點線面風)             6/14 高雄（可改名）        去哪（GPS/點地圖/選填名）
＋縣市足跡著色(淡)            當天時間軸：               一句話
出遊卡串（新→舊）              停留●──途經○──停留●       跟誰（角色館的人，多選）
想去清單(摺疊)                 每點：名稱/一句話/塗鴉/照片  順手記一筆錢(摺疊,選填)
[＋記一筆] 唯一的大按鈕        當天花費小計               塗鴉(摺疊,選填)
                             [補一筆到這天]              → 存檔回首頁，零確認頁
```

- **手札首頁**＝book/ 台灣頁的完全體：地圖與出遊卡串互相點亮；縣市去過幾次直接淡淡著在島上（stats.html 併入）；想去是摺疊清單＋地圖空心點。
- **出遊卡**＝回顧單位：自動成串（同一天）、預設名「日期＋城市錨點」可改、停留/途經自動判(站/高鐵/公車/機場)可手動改（**兩邊都留**：打卡當下能切，事後在出遊卡也能改——已定案）、當天花費小計來自順手記的錢。
- **記一筆**＝唯一的輸入口：打卡/想去/花費不再是三個 type 三種表單，而是一張表——「去哪＋一句話」必填 30 秒內完事，錢和塗鴉是摺疊的選填段。想去＝同一張表勾「還沒去，想去」。
- **個人制**：author 固定是角色館標 ME 的人，「跟誰」從角色館選；舊資料 who=成員 → author=我、with=[成員] 規則遷移。
- **離線**：沿用 PWA；送出失敗進佇列（冰島版那套抽過來的時機）。

**依賴的共用模組**（寫這個小本的過程順序把它們逼出來）：
地圖引擎（從 book/ 抽）→ 打卡表單（新寫）→ 記帳引擎 splitMode:none（新寫核心，出國版之後接同一核心）。

### 記帳引擎第一原則：類別定義只有一份（2026-09-06 的教訓）

冰島版帳本接「保險」類別時，同一件事改了四輪才齊：進度條、圓餅切片、
圖例、每人應付明細——因為**類別清單在四個呈現點各抄一份**，數字計算集中
但呈現各自為政，加類別／改規則就得四處手動跟進，漏一處就是一個 bug 回報。

統一版的記帳引擎規格：

```js
CATEGORIES = [
  { key:'car',    label:'租車', color:'#f0c040', modes:{ shared:'total',  member:'/3'   } },
  { key:'flight', label:'機票', color:'#4fc3f7', modes:{ none:'hide', equal:'/3', member:'own' } },
  { key:'ins',    label:'保險', color:'#26c6da', modes:{ none:'hide', equal:'each', member:'own' } },
  ...
]
```

- 每個類別一行：名字、顏色、**三種視角下的取值規則**（total／÷3／own／each＝各自／hide）
- 進度條、圓餅、圖例、明細**全部從這張表產生**；加類別＝加一行，四處自動跟上
- 視角規則寫在類別身上，不是散在呈現層的 if
- **2026-09-06 更新：冰島版已經照這個原則重構完成**（render-ledger.js 的
  `LEDGER_CATS`＋`calcLedger()`），連 render.js 首次渲染和 scene.js 預算條
  的另外兩份複製體也一併收攏。新引擎可以直接把這份實作當底。

### 國內/國外怎麼整合（2026-08-31 定案）

**資料層整合、呈現層分類；分類的軸是「重規劃/隨興」，不是「國內/國外」。**

- **資料層同一套**：打卡/花費/角色/旅程不分國內外，欄位一模一樣；
  記帳同一核心，只差 `splitMode: full/none`。跨兩邊的統計
  （今年出門幾天、跟誰同行幾次、角色館出場數）因此才算得出來。
- **旅程/出遊有 `mode: planned | casual`**，兩本書是兩個 mode 的檢視：
  大本＝planned（行前準備/航班/住宿/分帳這些重東西的家），
  小本＝casual（摩擦力極低的隨手記）。
- **地圖跟著座標走，不跟著書走**。反例驅動的設計：
  - 隨興的沖繩三天 → casual，進小本，落在世界地圖（逼它建大本旅程
    ＝重演舊台灣版「小旅行也要建旅程」的錯）
  - 認真規劃的環島十天 → planned，進大本，落在台灣地圖
- **預設值偷懶但不是牆**：在台灣的記錄預設 casual、出國預設 planned，可改。
- **實作上的唯一禁令**：不要把「台灣」寫死進小本的資料模型。
  小本首頁仍是台灣地圖（99% 情境），偶爾有國外 casual 點就在角落
  多一顆小地球入口。

### 已經抽出來的（2026-08-31，第一批）

| 檔案 | 內容 | 誰在用 |
|---|---|---|
| `shared/theme.js` | 九款佈景主題、`Theme.mount()` 自動長出主題鈕，選擇存 `book_theme` 全站共用 | book/、roster/ |
| `shared/roster.js` | 角色資料模型與存取（`Roster.all/get/upsert/save/spriteSvg/startTicker/addStats/exportJSON`） | roster/、book/ |
| `shared/sprites.js` | 成員像素圖（從 taiwan/sprites.js 複製，之後以這份為準） | roster/、book/ |
| `roster/index.html` | 角色館頁面：地平線舞台＋角色卡＋編輯（含 pixelpad 畫板）＋匯出匯入 | 書桌的門進入 |

角色欄位：`id / name / alias / color / icon / spriteKey / sprite / intro / tags / isMe / since / refs`。
`id` 是永久識別碼，改名不影響既有紀錄；`isMe` 是打卡個人制的 author 預設值（只能有一位）。
`refs` 放各 app 的專用欄位（冰島版的 sceneId/charId/bubbleId）。

**儲存目前是 localStorage（`roster_v1`）**，只在自己這台裝置。接後端時只要換掉
`roster.js` 的 `load()/save()`，其餘介面不動。出場統計用 `Roster.addStats(fn)` 由各頁
自己餵資料源，角色館只負責顯示。

版面是**格鬥遊戲選角**：左邊一大格放被選中的人，右邊 n×n 選格（自己一個容器，
一頁 12 位，滿了翻頁），選中的用四個角的遊標框住。換人有三種：大格的 ‹ ›、
鍵盤方向鍵、手機在彈窗上左右滑。手機才是彈窗，桌機一直攤在左邊。

#### 角色館可能長成的樣子（使用者的「也許」，先記著不做）

> 這以後就會是我的人脈管理紀錄……也許
> 還可以放檢測量表，檢測他是否是個好旅伴，適合怎樣的事務……也許

現有欄位已經朝這個方向留了空間：`alias`（稱號）、`tags`（特徵）、`since`（何時認識）、
`intro`（介紹）。若真要走人脈／旅伴評估：

- **量表放在角色底下，不是另一個系統**：幾個軸（作息、預算、決策速度、體力、
  導航或社交誰負責…），存成 `traits: { key: 1–5 }`，角色卡上畫成簡單的線圖。
- **評分的來源要是自己的觀察，不是問卷**：每趟旅程結束後補一句「這趟他負責什麼、
  順不順」比要人填表實際；可以由出遊卡帶出「這趟跟誰」再快速標記。
- **不要做成評價人的工具**：只有自己看得到（本來就是個人制），文字用
  「適合一起做什麼」而不是好壞分數。
- 真正有用的產出是**組隊建議**：規劃新旅程時，依這趟的性質（開車多／早起／預算緊）
  提示適合找誰，這才是量表的價值，不然只是填了沒用的資料。

先不實作，等統一版把旅程與出遊資料接起來、有東西可回顧了再談。

### 封存分兩批（時間點不同）

1. **taiwan/ 可以早封存**：資料全是歷史紀錄，沒有「行程中會變動」的問題。
   新小本一能讀就把舊的凍成唯讀或下架。
   資料先遷進新模型：`member` → `author=我` + `with[]`。
   台灣版是**重寫不是搬**（結構本身錯了，見「台灣版重構方向」）。
2. **iceland/ 不能提早封存**：旅途中是活的，要 2026/10 回來、帳結完
   才把 sheet 凍成靜態 JSON、頁面轉唯讀。

### 封存時務必檢查

- **旅伴用的網址不能變**：root `index.html` 目前轉址到 `iceland/`，
  那是發給猴花寧的連結。封存後這個路徑要繼續指到封存頁（或新系統的對應頁）。
- 行前兩週（8/31–9/14）新專案可以慢慢長，但**不要吃掉行前待辦的時間**。

## 未來功能（先記著，不急）

- **上傳訂房頁自動填表**：瀏覽器 `DOMParser` 就能做，不需要後端。
  實測可靠度：名稱／地址／座標／金額／入住時間／設備抓得穩；
  **付款方式抓不到**（卡片是圖片沒有 alt）、**語意歸屬會錯**（「只收現金」在講住宿還是加購項目分不出來）。
  定位成「自動填 80%，人工核對」，不要設計成全自動。
- 若要用 LLM 抽資料（抗改版、能判斷語意），需要後端藏 API key — Railway 剛好可以。

---

最後更新：2026-08-29，冰島出發前 16 天的規劃討論。

## 佈景主題系統（已定案的設計決定）

**配色不是選一個，是做成可切換的佈景主題。** 整個介面由六個顏色變數驅動
（底 space／墨線 ink／淡線 faint／出國網點 dotA／手札網點 dotB／暖點 spark），
主題＝一組六色，存 localStorage，設定頁可換——跟冰島版的日夜切換同一套機制。

九款起手（設計稿已調好，值直接抄）：

| 主題 | space | ink | faint | dotA | dotB | spark |
|---|---|---|---|---|---|---|
| A 大地 | #f7f1e6 | #5b4a38 | #b9ab94 | #cf9d7d | #b3ad7e | #c97b3d |
| B 墨青 | #f4f3ee | #3f4b55 | #a8b0b5 | #9fb3c0 | #b9bfa3 | #c05a41 |
| C 苔原 | #f3f0e2 | #494a35 | #adab8d | #a9ad72 | #cbb289 | #bf6b35 |
| D 藍晒 | #f2f3f0 | #2f4858 | #9fb0b8 | #8fb0c4 | #b7c2ad | #d08c2e |
| E 燒陶 | #f9f1e5 | #67402b | #c4a68d | #d68f6a | #d9c194 | #a83b2c |
| F 石墨 | #f0eeea | #4c4a47 | #b1aca3 | #b5aa9b | #a8aaa2 | #d29e3a |
| G 梅子抹茶 | #f6f2ea | #4e4553 | #b3a8ac | #d8a0b0 | #a9bd8f | #c9a53c |
| H 鮭魚湖水 | #f7f3ec | #46524e | #a3b0aa | #e2a68f | #93bfb2 | #9a6b8f |
| I 藕紫芥末 | #f4f1ea | #52493f | #b0a795 | #b9a3c4 | #cdb168 | #bf5f4a |

視覺方向（設計稿 https://claude.ai/code/artifact/ccf1d598-b317-47dd-8e90-7e38389672d2 ）：
純淨空間中的點線面。書＝網點面（漫畫網版）、旅程＝手寫微顫線（實線走過／虛線未走）、
地點＝點（唯一暖色的那顆會呼吸＝下一趟）。不寫標題字，用抽象線圖：
出國＝虛線弧越過地平線；手札＝閉合環裡兩點相連。

## 打卡改個人制（已定案的資料模型決定）

**打卡只有我一個人寫，由我記錄「身旁有誰」；不開放其他人編輯。台灣版／國際版一體適用。**

- `checkin.author` 固定＝我（不再是「選你是誰」的成員選擇器）
- 新欄 `checkin.with`＝同行者清單（多選，從成員名冊挑，可空＝獨旅）
- 打卡表單的成員列語意改變：從「我是誰」→「**誰在場**」
- 寫入不需要多人帳號權限——整個系統單一作者，其他人是被記錄的對象
- 分帳照舊需要成員（記帳跟打卡是兩回事：帳有多人分攤，腳印只有一支筆）

**現有兩個 App 的處理：**
- 台灣版：實際上本來就只有我在寫，資料相容；正式改版落在統一版，不動舊程式
- 冰島版：出發在即不動它。旅途中照現在的方式用（選成員打卡），
  回來遷移到統一版時，把舊打卡的「成員」欄位換算成 author=我 + with=[那個成員] 即可

## 網站結構 v2（2026-08-31 重新討論的結論）

### 台灣版「用起來茫然」的診斷（樣本：6/14 高雄一日遊的 8 筆真實打卡）

1. **記錄單位錯了**：打的是「點」，回想的是「那天」。「出遊」這個物件不存在，
   地圖只是歷來散點的堆積，沒有故事、沒有入口。
2. **點的性質混雜**：交通節點（六家站/左營高鐵/公車站/車上）、真想記的地方
   （華僑市場/海上教堂咖啡）、時刻（貝殼姐/腳痛叫車）畫成同一種點，噪音淹掉訊號。
3. **地點欄被當萬用欄**：「車上」「貝殼姐」是想記的一句話，不是地點。
4. **回顧沒有入口**：打開就是全部，沒有「上次那天」。

### 小旅行記錄模型（對症）

- **出遊（outing）自動成串**：同一天的打卡自動綁成一趟，零額外操作（分組用算的）。
  地圖上一趟出遊＝一條路線＋一個代表點，點開才看當天細節。
- **點分兩級**：停留 vs 途經。名稱含 站/高鐵/公車/機場 自動歸途經（路線上的小空心點），可手動改。
- **「一句話」一等公民**：可不填地點直接記，自動掛進當天出遊。
- **回顧單位＝出遊卡**：日期＋去了哪＋點數＋花費＋那幾句話。
- **出遊名稱：先預設、可修改**（2026-08-31 定案）。自動用「日期＋主要城市錨點」
  生名字（如「6/14 高雄」），想改就改成「陪貝殼姐回診」這種自己的名字。
- **停留／途經判錯：兩邊都能改**（2026-08-31 定案）。
  - 打卡當下：表單上一個小切換（停留 ⇄ 途經），自動判定的結果先填好，勤勞時順手改。
  - 事後：出遊卡的時間軸上每個點都能直接切，懶得當下處理時回頭批次整理。
  - 一旦手動改過就標記 `manual:true`，之後自動判定不再覆蓋它。

### 台灣版重構方向（2026-08-31 補充）

使用者原話：「點進台灣版的頁面會看不懂要幹麼」「功能不少 但不直覺」
「需要拆解重構，看看怎麼跟共用整合，或乾脆把國際/國內透過首頁更加融入地整合」。

現況盤點（taiwan/index.html 單檔 2674 行）：一張 Leaflet 地圖上疊了
**11 個 modal**（打卡/想去/記帳三合一表單、旅程列表、旅程表單、帳本、名冊、
角色編輯、行前 QA 小卡、旅程詳情、卡片編輯、留言、畫板），入口全是浮動按鈕，
打開頁面沒有任何「現在該做什麼」的引導 → 這就是不直覺的根源：
**一頁塞了三個產品**（打卡地圖＋記帳＋旅程規劃），彼此還互相藏在 modal 裡。

重構原則（併入統一專案，不另起爐灶）：

1. **台灣版不再是獨立 app，而是旅程書的小本**。入口統一走首頁書桌，
   `/notes` 打開就是「出遊卡列表＋台灣地圖」，不是功能大雜燴。
2. **一頁一件事**：
   - 打開小本 → 看出遊卡（回顧）
   - 「記一筆」→ 只問去哪＋一句話（記錄）
   - 記帳 → 共用記帳引擎的頁面，不再塞進打卡表單
   - 旅程規劃/行前小卡 → 那是大本的事，從小本移除
3. **能共用的全部上繳書桌層**：名冊/角色編輯 → 角色館；記帳 → 記帳引擎；
   畫板/sprite → 角色館附屬；主題 → 主題系統。小本自己只剩：
   出遊卡、記一筆、台灣地圖、想去清單。
4. **現有 taiwan/ 在冰島行前凍結不動**，重構發生在新專案裡，
   資料照舊從 sheet 遷移（member→author+with 規則照前面定案）。

### 書桌層共用模組（兩本書共用）

| 模組 | 內容 | 現況 |
|---|---|---|
| 角色館 /roster | 遊戲人物頁：sprite動畫/代表色/介紹＋出場統計（出遊次數/腳印/分帳往來）；兩本書用 member_id 引用 | ✅ 已實作（roster/ ＋ shared/roster.js） |
| 記帳引擎 | 同一核心，splitMode: full(出國)/none(島內) | 兩版各一套，要合 |
| 打卡表單 | 個人制 author=我 + with=誰在場；停留/途經兩級 | 已定案 |
| 地圖引擎 | pins/合併計數/路線/城市錨點/離線快取 | book/ 的實作是雛形 |
| 主題系統 | 九款六變數 | ✅ 已抽成 shared/theme.js（book/ 與 roster/ 共用） |
| 離線佇列＋同步 | 冰島版那套 | 抽出即用 |
| schema 表單引擎 | 之前定案 | 待做 |

### 相片功能（確認可行的路徑）

- EXIF 的 GPS＋拍攝時間**瀏覽器就能讀**（File API，不用後端）
- 流程：選照片 → 自動落地圖 → 用時間比對自動歸進出遊
- 儲存沿用**冰島版現有的 R2 上傳 Worker**
- 這同時把旅程書變成「按出遊整理的相簿」，解決手機相簿雜亂

### 路由草案

```
/          書桌：紙（總覽）＋兩本書＋角色館的門＋主題鈕
/tour      大本：世界地圖 → 旅程頁（規劃/執行/封存）
/notes     小本：台灣地圖 → 出遊卡 → 當天時間軸（點＋照片＋一句話＋花費）
/roster    角色館
shared/    共用模組
```
