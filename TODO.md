# 待做

等「旅途」分頁的資料（日程／住宿／活動／保險）都填完整之後再動工。

## 地圖 × 旅途 連動

### A. 地圖加兩個圖層：📅 日程、🎯 活動
- 圖層選單多兩個開關（沿用現有 `toggleFilter` 機制）
- 資料來源：`日程` 表已有 緯度／經度欄；`活動` 表已有集合點座標
- GAS 需要多回一個帶座標的 endpoint（或擴充現有 `?sheet=mapdata`）

### B. 兩邊互相導覽，但行為不對稱
- **在行程頁**點「看位置」→ 不跳轉到地圖頁，而是**彈窗只顯示位置**（小地圖預覽）
- **在地圖頁**點日程／活動的 pin → 打開**住宿／活動的詳細卡片**（重用 `openSchStay` / `openSchAct` 那兩張彈窗，不另做）

### C. 計畫線 vs 實際線
- 現有「足跡連線」＝實際打卡順序（實線）
- 再加一條**虛線的計畫路線**＝日程表當天節點的順序
- 兩條疊在一起看得出「原本要去哪 / 實際跑去哪」
- 冰島回來、實際打卡資料夠多之後才有意義

## 其他待確認

- 「雜」分頁：Kkday（用途不明）、未識別座標 64°45'25.5974"N 21°48'40.0514"W（≈64.7571, -21.8111）
- `保險` 分頁還是空的，等買完保險填入
- 出發前清掉在台灣測試用的打卡／記帳資料
